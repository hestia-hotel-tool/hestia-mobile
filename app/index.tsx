/**
 * Phase 2 - Expo Router entry point.
 * Renders the existing App with its own NavigationContainer,
 * embedded inside Expo Router's root NavigationContainer.
 *
 * This keeps all old navigation working while we gradually
 * migrate screens to file-based routes.
 */
import App from '../src/root/App';
export default App;
