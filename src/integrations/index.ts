/**
 * Third-party integrations (PMS, …).
 *
 * Each integration lives under `integrations/<vendor>/` and exposes a
 * provider-agnostic facade. See `integrations/pms` for the reference pattern.
 */
export * from './pms';
